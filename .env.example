# Where the API lives. Leave blank in development — Vite proxies /api to
# http://localhost:5000 automatically (see vite.config.ts).
#
# In production set this to your deployed API, e.g.
#   VITE_API_URL=https://api.your-domain.com
VITE_API_URL=
